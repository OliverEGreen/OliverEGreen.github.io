title: Ollie Green
description: "Against my better judgement, thinking sometimes still happens: it's a problem we're working on. Some of it leaks out into the environment here."
author: Ollie Green
url: "https://olliegreen.info"
baseurl: ""

# Build
markdown: kramdown
highlighter: rouge
permalink: /writing/:title/
timezone: Europe/London

# Plugins (all on the GitHub Pages allowlist)
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

# Defaults
defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: post

# Files Jekyll should not copy into _site
exclude:
  - Gemfile
  - Gemfile.lock
  - vendor
  - .bundle
  - README.md
  - .DS_Store
  - .gitignore
  - scripts
