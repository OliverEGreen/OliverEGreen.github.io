---
permalink: /assets/js/404-redirect.js
sitemap: false
---
(function () {
  // Post URLs are canonical by ID: /posts/<id>/<slug>/. Any other slug (or none)
  // under a known ID lands on the 404 page, which loads this script to redirect
  // to the canonical URL. The map below is generated by Liquid at build time.
  var posts = {
    {%- for post in site.posts %}
    "{{ post.post_id }}": "{{ post.url | relative_url }}"{% unless forloop.last %},{% endunless %}
    {%- endfor %}
  };
  var match = window.location.pathname.match(/^\/posts\/(\d+)(?:\/.*)?$/);
  var canonical = match && posts[match[1]];
  if (canonical) {
    window.location.replace(canonical + window.location.search + window.location.hash);
  } else {
    var links = document.getElementById("not-found-links");
    if (links) {
      links.hidden = false;
    }
  }
})();
